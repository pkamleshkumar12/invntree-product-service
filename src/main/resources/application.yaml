spring:
  application:
    name: INVNTREE Product Service
  datasource:
    url: jdbc:mysql://localhost:3306/invntree_db
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8083
